#############################
# Build System Requirements #
#############################

[build-system]
requires = [
  "setuptools >= 52.0.0",
  "wheel >= 0.36.2",
]
build-backend = "setuptools.build_meta"
